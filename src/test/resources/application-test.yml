spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true
  data:
    redis:
      host: localhost
      port: 6379
  mail:
    port: 465
    username: FAKE
    password: FAKE
    host: localhost
    request-uri: FAKE
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: false
          ssl:
            enable: true
            trust: FAKE
        transport:
          protocol: smtp
        debug: true

---
cloud:
  aws:
    credentials:
      accessKey: FAKE
      secretKey: FAKE
    s3:
      bucket: FAKE
    region:
      static: FAKE
    stack:
      auto: false

jwt:
  secret: FAKE