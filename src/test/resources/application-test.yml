spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true
  data:
    redis:
      host: localhost
      port: 6379
---
cloud:
  aws:
    credentials:
      accessKey: FAKE
      secretKey: FAKE
    s3:
      bucket: FAKE
    region:
      static: FAKE
    stack:
      auto: false

jwt:
  secret: FAKE